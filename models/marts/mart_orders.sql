with source as (
    select
        order_id,
        customer_id,
        order_date,
        status,
        subtotal,
        tax_amount,
        shipping_cost,
        discount_amount,
        total_amount,
        currency,
        order_item_count,
        total_quantity,
        gross_item_revenue,
        net_item_revenue,
        carrier,
        shipping_method,
        shipping_status,
        days_to_ship,
        days_late,
        is_late,
        shipping_performance_bucket,
        total_paid_amount,
        primary_payment_method,
        payment_status_summary,
        is_completed_order,
        is_cancelled_order,
        is_refunded_order,
        is_paid
    from {{ ref('int_orders_enriched') }}
)

select
    order_id,
    customer_id,
    order_date,
    status,
    subtotal,
    tax_amount,
    shipping_cost,
    discount_amount,
    total_amount,
    currency,
    order_item_count,
    total_quantity,
    gross_item_revenue,
    net_item_revenue,
    carrier,
    shipping_method,
    shipping_status,
    days_to_ship,
    days_late,
    is_late,
    shipping_performance_bucket,
    total_paid_amount,
    primary_payment_method,
    payment_status_summary,
    is_completed_order,
    is_cancelled_order,
    is_refunded_order,
    is_paid
from source
